id: opt_pid_controller_no_disturbances
execute:
  cmd: "roslaunch uuv_smac_opt_pid_no_disturbances start_smac_pid_default.launch"
  params:
    record: true
    # Set this flag to true to allow Gazebo to start its front end. Otherwise, the simulation will run without the
    # 3D visualization
    gui: false
    timeout: 105
