id: test_run_task
execute:
  cmd: "roslaunch uuv_evaluation example_mission.launch"
  params:
    record: true
    # Set this flag to true to allow Gazebo to start its front end. Otherwise, the simulation will run without the
    # 3D visualization
    gui: false
    timeout: 5
